{
  "name": "react-perfscope-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --filter=./packages/* typecheck",
    "api:check": "node tools/api-surface/surface.mjs --check",
    "api:update": "node tools/api-surface/surface.mjs --write"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "happy-dom": "^20.9.0",
    "tsup": "^8.0.0"
  }
}
